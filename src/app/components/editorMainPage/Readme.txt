Readme.txt

Pre-Requise:
This page is only visible if the user have a role of "Manager", otherwise it will be move to the main Dashboard or Login page. 

Info:
This page will get the MapReduce info from CouchDB and display the info. 
The info I will receive from the Mapreduce is the list of all type of doc available. 

Action: 
Then the manager can select a "Type" of doccument and go into the "DocEditor" page.

Note: The role of a user in CouchDB can only be set by a Admin.